config:
  target: "http://localhost:3008"
  phases:
    - duration: 1 # run for 1 seconds
      arrivalRate: 5000 # 10 users per second
      name: "Load test cluster"

scenarios:
  - flow:
      - get:
          url: "/"
